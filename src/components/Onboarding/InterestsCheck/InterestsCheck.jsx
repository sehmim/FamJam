import React from 'react';
import { Grid, Typography, SelectField } from '@material-ui/core';


export default function InterestsCheck(props) {

  return (
    <React.Fragment>
      <Typography variant="h6">
        InterestsCheck
      </Typography>
      <Grid item xs={12} sm={6}>
        <Typography variant="h6">
            TODO
            This is where you choose genres and interests to get matched with people
        </Typography>
        </Grid>
    </React.Fragment>
  );
}
